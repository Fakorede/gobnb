{{template "base" .}}

{{define "content"}}
  <div class="container">
    <div class="row">
      <div class="col">
        <h1>This is the about page</h1>
        <p>Some Text</p>

        <p>This is some dynamic data from the handler: {{index .StringMap "test"}}</p>
      </div>
    </div>
  </div>
{{end}}
